#!/bin/sh

# st -t notas -e tmux new -s notas &
/usr/local/bin/st -t diversos -e tumxp load "$HOME"/.tmuxp/pessoal.yml
# obsidian &
anki &
spotify &
qutebrowser &
# /usr/bin/dash $HOME/.scripts/brave-pessoal &
rm $HOME/Downloads
