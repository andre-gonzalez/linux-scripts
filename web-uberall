#!/bin/sh
BROWSER=$1
URL=$2
WINDOW_NAME="web-uberall"

case "$BROWSER" in
	brave) brave --profile-directory="Profile 3" --window-name=$WINDOW_NAME --app=$URL ;;
	thorium) thorium-browser --window-name=$WINDOW_NAME --app=$URL ;;
	zen) zen-browser ;;
	chromium) chromium --profile-directory="Default" --window-name=$WINDOW_NAME --app=$URL ;;
	helium) helium-browser --window-name=$WINDOW_NAME  --profile-directory="Default" --app=$URL ;;
	*) ${BROWSER:-helium-browser} --window-name=$WINDOW_NAME  --profile-directory="Default" --app=$URL ;;
esac
